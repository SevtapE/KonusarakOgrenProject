@model Application.Features.Exams.Models.ExamListModel

<table class="table">
  <thead>
    <tr>
      <th scope="col">Id</th>
      <th scope="col">Title</th>
      <th scope="col">Creation Date</th>
        <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
     
      @foreach(var exam in Model.Items)
      {
            <tr>
          <td>@exam.Id</td>
         <td>@exam.ArticleTitle</td>
     <td>@exam.CreationDate</td>  
     <td>   <button type="button" class="btn btn-secondary">Sil</button></td>
       </tr>
    
      }

  
 
  </tbody>
</table>


@*@foreach(var exam in Model.Items){
    <li>@exam.Id</li>
         <li>@exam.ArticleTitle</li>
     <li>@exam.CreationDate</li>    
}*@
